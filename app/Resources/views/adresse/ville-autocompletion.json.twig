[{% for item in results -%}
    {{ {id: item.placeName|upper|e, text:  item.placeName|upper|e, value: item.placeName|upper|e}|json_encode|raw }}
    {# use "value" instead of "id" key, if you use jquery-ui #}
    {%- if not loop.last %},{% endif -%}
{% endfor %}]